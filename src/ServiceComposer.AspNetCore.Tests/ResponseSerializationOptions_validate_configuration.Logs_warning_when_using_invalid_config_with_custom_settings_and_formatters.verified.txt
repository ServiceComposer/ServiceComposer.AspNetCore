ResponseSerialization UseOutputFormatters is set to true, and it's also configured to use either a custom response casing or custom json serializer settings. When using output formatters, custom settings are ignored